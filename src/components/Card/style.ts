import styled from "styled-components";
export const Container = styled.div`
    padding: 5px;

    .card-box {
        display: flex;
        align-items: center;
        flex-direction: column;
        padding: 2rem;
        background: var(--box);
        border-radius: 1rem;
    }

`;